# Pinned mirror of pyproject.toml for environments that install from a
# requirements file. pyproject.toml remains the source of truth; regenerate
# this file if the dependencies there change.

# Runtime
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
SQLAlchemy==2.1.0
asyncpg==0.31.0
greenlet==3.5.6
pwdlib[argon2]==0.3.1
PyJWT==2.15.0

# Development
alembic==1.20.0
httpx==0.28.1
pytest==8.4.2
pytest-asyncio==1.4.0
python-dotenv==1.2.3
ruff==0.16.9
